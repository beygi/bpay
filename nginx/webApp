server {
       listen 80;
       listen [::]:80;

       server_name staging1.b2mark.com avazcoin.com;

       root /home/staging/web/dist;
       index index.html;

       location / {
                try_files $uri $uri/ /index.html;
       }
}
